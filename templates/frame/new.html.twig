{% extends 'base.html.twig' %}
{% block body %}
    <h1>Create new Frame</h1>
    <div class="card card-body">{{ include('frame/_form.html.twig') }}</div>
    <div class="d-flex flex-row justify-content-end">
        <div class="p-4">
            <a href="{{ path('app_frame_index') }}">back to list</a>
        </div>
    </div>
{% endblock %}
